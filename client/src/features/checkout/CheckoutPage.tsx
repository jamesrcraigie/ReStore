import { Typography } from "@mui/material";

export default function CheckoutPage() {
    return (
        <Typography variant='h2'>
        Only logged in users will be able to use this!
    </Typography>
    )
}